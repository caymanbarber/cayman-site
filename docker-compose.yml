services:
  backend:
    build: ./backend
    environment:
      - PORT=3000
      - STATIC_DIR=/srv/static
      - RUST_LOG=info

  caddy:
    build: ./frontend
    ports:
      - "8080:8080"
    depends_on:
      - backend
