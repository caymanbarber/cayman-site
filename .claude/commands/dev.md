Start the development environment. Run the Rust backend and TypeScript frontend dev server.

Steps:
1. In backend/: `cargo run` (starts API on :3000)
2. In frontend/: `npm run dev` (starts Vite on :5173 with proxy to backend)

If dependencies are missing, run `npm install` in frontend/ first.
